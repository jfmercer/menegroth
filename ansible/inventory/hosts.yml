---
all:
  children:
    ai_server:
      hosts:
        ai-server:
          # Bootstrap only: set to the server's public IPv4 (terraform output
          # server_ipv4) for the first run. Phase 3 replaces this with the
          # Tailscale MagicDNS name once the host goes dark.
          ansible_host: REPLACE_WITH_PUBLIC_IP
          ansible_user: admin
