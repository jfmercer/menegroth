---
all:
  children:
    ai_server:
      hosts:
        ai-server:
          # MagicDNS short name — resolves for any device on the tailnet,
          # including CI runners joined via tailscale/github-action. The
          # host has no public inbound access.
          ansible_host: ai-server
          ansible_user: admin
